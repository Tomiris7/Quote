import {FC} from "react";import {TQuote} from "../../types/quote.ts";import styles from "./Quote.module.css"interface IQuoteProps extends TQuote {	deleteQuote: (category: string, id: string | undefined) => void	editQuote: (id: string | undefined, category: string, author: string, quoteText: string) => void}export const Quote:FC<IQuoteProps> = ({id, category, author, quoteText , deleteQuote, editQuote}) => {	const handleDeleteQuote =() => {		deleteQuote(category, id)	}	const handleEditQuote = () => {		editQuote(id, category, author, quoteText)	}	return (		<div className={styles["quote-item"]}>			<div className={styles["btn-container"]}>			<button className={styles["btn"]} onClick={handleEditQuote}>edit</button>			<button className={styles["btn"]} onClick={handleDeleteQuote}>X</button>		</div>			<div className="title">				<p className={styles["quote-inner"]}><span className={styles["title-name"]}>Author: </span>{author}</p>			</div>			<div className="quote-text">				<p className={styles["quote-inner"]}><span className={styles["title-name"]}>Quote: </span>{quoteText}</p>			</div>		</div>	)}